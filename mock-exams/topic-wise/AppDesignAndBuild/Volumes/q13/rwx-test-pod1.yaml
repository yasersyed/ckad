apiVersion: v1
kind: Pod
metadata:
  labels:
    run: rwx-test-pod1
  name: rwx-test-pod1
spec:
  volumes:
    - name: rwx
      persistentVolumeClaim:
        claimName: rwx-pvc
  containers:
  - image: nginx
    name: rwx-test-pod1
    resources: {}
    volumeMounts:
    - name: rwx
      mountPath: /data
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
