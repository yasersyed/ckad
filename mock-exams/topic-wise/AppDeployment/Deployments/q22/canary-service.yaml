apiVersion: v1
kind: Service
metadata:
  labels:
    app: canary-service
  name: canary-service
spec:
  ports:
  - name: 80-80
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: canary
  type: ClusterIP
status:
  loadBalancer: {}
